extends layout

block content
  h1 Verificación del cargo
  p El cargo por #{charge.amount / 100} pesos, tiene un estado: #{charge.status}
  p: img(src="#{charge.payment_method.barcode_url}")
  hr
  pre #{JSON.stringify(charge, null, 2)}
